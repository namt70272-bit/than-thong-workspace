# 2026-05-13 - Ngày Kiến Tạo Thần Thông

## Tổng quan
Xây dựng toàn bộ lớp điều hành nội bộ cho OpenClaw workspace, mang tên **thần thông**. Bắt đầu từ việc dọn dẹp workspace, tổ chức 16 mảng hệ thống từ `G:\Ai`, xây dựng toolchain nội bộ local-first, và mở rộng quản lý Windows.

## Những gì đã làm

### Sáng: Phân tích & Tổ chức
- Kiểm kê `G:\Ai` (189 files, ~6.59 GB)
- Tách 16 mảng hệ thống, mỗi mảng có cấu trúc rõ (Nguồn → A0 → A1 → B → Imports → Ghi chú)
- Đặt staging ngoài workspace: `E:\KY-DATA\OpenClaw\mang-he-thong\`
- Chắc lọc A0 cho 16/16 mảng — hoàn toàn an toàn, không runtime, không billing

### Trưa: Cleanup workspace
- Dọn file rác (`_fix_timeout.py.bak`, temp scripts)
- Gom file tạm vào `archive/deprecated/`
- Chuyển historical docs khỏi root → `references/workspace-governance/`
- Root chỉ còn file canonical
- Audit utils trùng chức năng (`rate_governor` vs `rate_limiter`, `prompt_loader` vs `prompt_utils`) → kết luận: giữ cả, phân vai rõ

### Chiều: Xây dựng Thần Thông
- **Giai đoạn billing → top → thần thông**: 3 lần đổi tên, cuối cùng chốt **thần thông**
- **Gate/Routing**: `billing_gate.py`, `task_router.py`, `preflight_runner.py`
- **Wrapper**: `billing_wrapper.py`, `than_thong_wrapper.py`, `than_thong_gate.py`, `than_thong_console.py`
- **Import pipeline**: `candidate_builder.py`, `import_validator.py`, `deep_validator.py`, `sync_executor.py`, `import_orchestrator.py`, `real_rollback.py`
- **Audit**: `duplicate_checker.py`, `canonical_checker.py`, `drift_checker.py`, `compliance_audit.py`, `bypass_risk_audit.py`
- **Windows-side (11 scripts)**: `top_win_audit.py`, `top_win_env_audit.py`, `top_win_svc_audit.py`, `top_win_startup_audit.py`, `top_win_process_audit.py`, `top_win_disk_health.py`, `top_win_cleanup.py`, `top_win_data_map.py`, `top_win_system_restore.py`, `top_win_tighten.py`, `top_win_full_dashboard.py`
- **Templates**: IMPORT-RECORD.md, A0-EXTRACTION.md, SYNC-RECORD.md
- **Operator layer**: `ops_console.py`, `ops_dashboard.py`, `top_console.py`, `than_thong_console.py`
- **Bảo trì**: `auto_maintain.py`, `than_thong_daily.py`, `top_scan_G_source.py`

### Import hoàn tất
- **16 mảng A0** (Level 1) + **16 mảng Level 2** (extract thật)
- Wave 1-4 đều qua pipeline: gate → candidate → validate → deep-validate → backup → sync → rollback
- ~25 candidate files, 25+ rollback manifests, 20+ sync records

### Windows-side
- Cron: `than-thong:auto-maintain` (6h sáng), `than-thong:win-audit` (7h sáng)
- Quét được ổ C, D, E, F, G
- Phát hiện E: skill có 3,142 SKILL.md

### Phát hiện mới
- `E:\skill` — 35,175 files, ~20 thư mục, ~800 official skills + ~1,950 community skills + ~6,400 cache files (~91MB)
- Có thể khai thác sau, cần lọc kỹ

## Kiến trúc hiện tại
```
E:\KY-DATA\OpenClaw\
├── mang-he-thong\            → staging gốc 16 mảng (189 zip gốc chưa xóa)
├── runtime-mirror\.openclaw\workspace\
│   ├── skills\than-thong\     → skill chính
│   ├── tools-internal\        → 44 script + policy + records + templates
│   │   ├── scripts\           → 44 .py entrypoint
│   │   ├── records\           → 40+ records JSON
│   │   ├── templates\         → 3 import/rollback templates
│   │   └── policy\            → billing.policy.json (machine-readable)
│   ├── tools-internal\policy\ → policies
│   ├── config\prompt-templates\ → 18 prompt packs đã import
│   ├── references\            → 16 receiving zones
│   ├── expansion\             → candidate + plans + queue
│   └── reports\               → THAN-THONG-TOAN-CANH.md
```

## Luật chính thức
- **thần thông**: tên chính thức cho lớp điều hành nội bộ
- **top + billing**: alias legacy để tương thích
- Local-first, anti-billing, gate trước mọi việc
- Import pipeline phải qua 3 lớp kiểm tra
- Ghi trong: AGENTS.md, TOOLS.md, references/compliance/THAN-THONG-RULE.md
